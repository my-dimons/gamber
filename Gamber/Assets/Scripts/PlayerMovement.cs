using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // v
        // vvv
        // if you want, DELETE THIS SCRIPT and do it yourself (maybe better for learning)
        // BUT DONT FORGET to comment code ok-ish (describe what a function is doing),
        // AND add nice, short, and descriptive commit messages (if there is more, add it to the commit description)
        // ^^^
        // ^


        // use WASD to move UP, LEFT, DOWN, and RIGHT
        // Adjustable variables for speed
    }
}
